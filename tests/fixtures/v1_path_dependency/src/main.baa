#تضمين "app_api.baahd"
#تضمين "local_lib.baahd"

صحيح الرئيسية() {
    إذا (قيمة_إضافية() != ٨) { إرجع ١. }
    إذا (قيمة_مكتبة() != ٤٢) { إرجع ٢. }
    اطبع "typed path dependency ok".
    إرجع ٠.
}
