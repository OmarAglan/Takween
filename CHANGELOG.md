# سجل تغييرات تكوين

يتبع هذا الملف Semantic Versioning ويفصل إصدار تكوين عن إصدارات CLI والبيان
وملف القفل.

## غير مصدر

### أضيف

- استهلاك بصمة `nazm-api-v1` الكاملة من بيان Baa وإدخالها في
  `takween-content-cache-v1` ودليل المفتاح؛ تغير إصدار نظم أو هضم قدراته يبطل
  الهوية، ويمكن الآن لوحدات باء المولدة و`.نظم` المباشرة تسجيل miss ثم hit
  حقيقيين بلا رجوع صامت.
- تثبيت smoke متعدد المنصات على Baa
  `4e250dad97d9d64de7f69ca96c8be48a34dc9ff5` ونظم
  `f7fcf8f6d2bf629daf708b3b6028e22c74683ce6` لاختبار عقد البصمة نفسه، لا مجرد
  أسماء أدوات متحركة.
- عقد `takween-build-events-v1` وخيار `--ملف_أحداث` لأوامر
  build/check/run/test/clean؛ يكتب JSONL متسلسلا للعملية والمراحل والأهداف
  والحزم والآثار في قناة منفصلة عن stdout/stderr، مع smoke لمسار عربي ذي مسافات.
- اعتماد نَظْم مجمعا إنتاجيا افتراضيا: البيان الذي يحذف `المجمع` يثبت
  `--assembler=nazm` في الخطة، ويتطلب قدرة `مصدر_نظم` ومسار نَظْم؛
  `المجمع = "غاز"` هو الرجوع الصريح ولا يفعّل عند فشل نَظْم.
- تثبيت workflow على Baa `661edd9...` وNazm `7236491...`. اجتاز commit
  Takween `da8378e...` بوابة Windows/Linux رقم `29689709002` بعد نجاح بوابة
  قبول Baa/Nazm الدقيقة رقم `29687846586`.
- عقد `takween-workspace-v1` وفهرسة أعضاء حزم explicit بأسماء عربية فريدة،
  مع `بناء/اختبار --حزمة` و`--كل_الحزم` وعزل cwd والقفل والكاش لكل عضو.
- تمرير profile/locked وكود خروج `compiler-cli-v1` عبر عملية مهيكلة لكل حزمة،
  ورفض globs والمسارات الخطرة/المكررة والمساحات المتداخلة.
- عقد `takween-content-cache-v1`: مفتاح SHA-256 حتمي يجمع خطة البناء وإصدار
  Baa والهدف والأعلام وتجزئات المصدر/الرؤوس/القفل، مع إيصال hit مجمع ودليل
  canonical قابل للفحص ومن دون ادعاء تخطي الربط؛ أضيفت لاحقا بصمة نظم لتمكين
  إعادة الاستخدام الآمنة لوحداته.
- DAG typed للأهداف بالحقل العربي `يعتمد_على`، مع قبول المكتبات فقط كعقد
  اعتمادية ورفض الهدف المفقود أو غير المكتبة قبل أي اكتشاف أو بناء.
- ترتيب topological حتمي dependency-first يدمج مصادر وتضمينات المكتبات المتعدية
  في argv المختارة، ويسجل `target_order` داخل `takween-build-plan-v1`.
- تشخيص دورة ثابت يطبع المسار العربي الكامل، وfixture
  `ألف -> باء -> جيم -> ألف` يثبت الرفض على Windows وLinux.
- أسماء أهداف v1 عربية فقط، مع smoke للهوية والحافة والربط والسلوك الانتقائي.
- نمطا `تطوير` (`-O1` مع التحقق) و`إصدار` (`-O2` مع التحقق) مدمجان، مع
  `[الأنماط.<اسم_عربي>]` للتخصيص و`--نمط <اسم>` للاختيار دون صيغة لاتينية.
- إيصال `profile` typed ومملوك داخل `takween-build-plan-v1`، ويشمل الاسم
  والتحسين والتحقق حتى يبني cache اللاحق مفتاحه من الخطة لا من حالة parser.
- مخطط بناء نقي في `المصدر/مخطط_البناء.baa` ينتج argv مملوكة بعد resolution
  دون process أو filesystem أو network، ويفصل تجهيز الخطة عن تنفيذها.
- عقد `takween-build-plan-v1` وأمر `تكوين خطة --جسون [هدف]` للفحص الآلي؛
  يثبت smoke تطابق الخطة بايتيا وعدم إنشاء الناتج وتثبيت اختيار `غاز/نظم`.
- alias عربي `--جسون` لعقود الخطة والأهداف، حتى لا يحتاج مستخدم الواجهة العربية
  إلى كتابة علم لاتيني؛ يبقى `--json` للتوافق الآلي.
- تثبيت اختيار المجمع صراحة في كل خطة: `--assembler=nazm` عند غياب
  `المجمع` أو اختيار `نظم`، و`--assembler=gas` عند اختيار `غاز`.
- جذور `.baa` و`.نظم` مختلطة في الصيغتين، مع رفض أي امتداد مصدر آخر.
- حقل v1 عربي typed باسم `المجمع` وقيمتي `نظم/غاز`; يمرر اختيار نظم إلى Baa
  ويستخدم المشغل العربي `نظم` على `PATH` بلا shell.
- استهلاك `target-info-v1.capabilities.nazm_source` وتسجيل `مصدر_نظم` في
  `takween-lock-v1`، مع smoke بناء/تشغيل ووحدات manifest مختلطة.
- محلل SemVer حتمي لإصدارات `major.minor.patch` وprerelease/build metadata، مع
  exact و`^` و`~` والمقارنات ومجالات AND واختيار أعلى إصدار مطابق وتعارض صريح.
- فهرس `takween-index-v1` محلي صريح عبر `TAKWEEN_PACKAGE_INDEX`، واختيار archive
  دقيق من أربعة حقول بلا مسار محتوى موثوق، مع تحقق SHA-256 بلا سجل عام.
- صيغة `takween-archive-v1` حتمية واستخراج آمن بحدود للملفات والأحجام والمسارات،
  يرفض المسارات المطلقة واجتياز الأب والروابط والتصادمات والترتيب غير الحتمي
  وتجزئة الملف الداخلية غير المطابقة.
- cache محتوى معنون بـSHA-256 وإيصال استخراج، مع إعادة تحقق وإصلاح للعبث من
  الأرشيف غير المتغير، ومقارنة مباشرة بسجلات الأرشيف تمنع تمرير إيصال مزور.
- `تحقق --بدون_شبكة` للتحقق الصارم من القفل والفهرس والأرشيف وcache دون جلب،
  و`توريد` حتمي إلى `مورد/<الاسم>/<الإصدار>/<sha256>`.
- قيود Baa والهدف والقدرات في البيان والقفل، وتطبيقها على المشروع واعتمادياته.
- أسماء عربية فقط للمشروع والحزم وأسماء الاعتماديات، مع رفض الأسماء اللاتينية
  والمختلطة قبل الحل.
- تسجيل قيد الإصدار ومسار الفهرس والأرشيف وSHA-256 والاختيار الدقيق داخل
  `takween-lock-v1`، مع رفع resolver إلى `0.2.0`.
- خيار `--locked` لـ build/check/run/test يقارن الحل الحالي بالقفل حتميا ولا
  ينشئ أو يستبدل القفل عند غيابه أو قدمه، ويحذف مرشح المقارنة المؤقت.
- smoke لحالات SemVer exact/caret/tilde/range/prerelease/build metadata، وثبات
  القفل، ورفض العبث بالأرشيف وcache وكل فئة أرشيف غير آمنة، وثبات التوريد، ونجاح
  التحقق دون شبكة بعد الإصلاح.

- `--إصدار/--version` مع فحص تطابق إصدار المصدر أثناء البناء.
- اختيار صريح لمسار Baa وstdlib في `build_takween.ps1`.
- runner آلي لـ help/version/init/build/run/clean وحالات الرفض الأساسية.
- تصميم `takween-manifest-v1` typed ومعمارية اعتماديات محلية أولا وملف قفل.
- `فحص/check` بتمرير مباشر لعقد `diagnostics-json-v1`.
- بناء incremental مع `.takween-cache` و`build-manifest.json` schema 1.
- Process API مهيكلة من Baa لـ argv/cwd/stdout/stderr/exit/cancel، مع إزالة
  `cmd /c` و`نفذ_أمر` من executor بالكامل.
- parser تلقائي لـ `takween-manifest-v1` بقيم نصية وصحيحة ومنطقية ومصفوفات نصوص،
  وأقسام المشروع/الهدف/البناء/النمط/الاعتماديات/مساحة العمل.
- اعتماديات `المسار` المحلية والمتعدية: قراءة بيان الحزمة، إضافة مصادرها ومسارات
  تضمينها إلى خطة Baa، كشف الدورات المباشرة، ومنع تكرار الحزمة في الرسم.
- fixtures smoke لـ v1 واعتمادية مسار ومخرجات تحوي مسافة و`&` ورفض مفتاح مجهول.
- أهداف v1 متعددة مع اختيار build/check/run، وأمر `test` لهدف واحد أو كل أهداف
  الاختبار، وعقد فهرسة `takween-targets-v1` لتكامل Qalam وCI.
- اعتماديات Git مثبتة بحقل `commit` exact، تُجلب وتُفحص عبر argv بلا shell وتخزن
  في `.takween/packages/<commit>` مع تعطيل checkout hooks والبروتوكولات غير المعتمدة.
- `takween-lock-v1` حتمي بـ UTF-8/LF للمشروع وهدف Baa وعقد path/Git المتعدية؛
  يغطي smoke ثبات البايتات وإعادة استخدام cache offline ورفض branch والمصدر المختلط.
- أكواد خروج موحدة مع `compiler-cli-v1` (`0` إلى `5`) وتمرير كود Baa كما هو عبر
  check/build/run/test، مع smoke مستقل لكل تصنيف على Windows وLinux.

### تغيّر

- عزل تحقق أسماء واعتماديات الأهداف عن frame محلل v1 الكبير، وإضافة stress من
  200 process لعقد الأهداف، وفصل executables المزيفة لأكواد Baa لمنع قفل Windows.
- منع تكرار حقول النمط ورفض هوية النمط اللاتينية أو المختلطة، مع إبقاء
  `--profile` alias توافقيا للأتمتة فقط.
- نقل ملفات اكتشاف الهدف في أمر `خطة` إلى `.takween/plan-info` حتى لا ينشئ
  التخطيط مجلد المخرج؛ يبقى build يسجل `target-info-v1` داخل كاش البناء.
- إغلاق بوابة 0.2 هندسيا بعد نجاح smoke Windows/Linux رقم 29258386560 مع Baa
  CI رقم 29258365418 وQalam CI رقم 29262024716؛ لا يغيّر ذلك رقم الإصدار المنشور
  قبل قطع حزمة إصدار مستقلة.
- تحديث خط أساس التكامل في مساحة Eco من Baa 0.4.4.1 إلى 0.6.0، وإثبات
  help/version/init/build/run/clean عبر runner ويندوز بمسارات عربية.
- جعل تضمين `تكوين.baahd` نسبيا إلى ملفات المصدر بدلا من الاعتماد على cwd.
- إعادة ترتيب الخارطة: build system متعدد المنصات، ثم manifest/DAG، ثم
  اعتماديات محلية وقفل، ثم سجل عام مؤجل.
- استبدال فلترة محارف shell بالحفاظ الحقيقي على حدود argv؛ تبقى فحوص اجتياز
  المسار والأعلام الحرة typed، ولا تعاد قراءة أي قيمة كأمر shell.

## 0.1.0 - 2026-03-03

- MVP خاص بويندوز: init وparser صارم وbuild/run/clean ومثبت Inno Setup.
