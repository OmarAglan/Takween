# خارطة طريق تكوين (Takween Roadmap)

## الهدف

تكوين هو مالك تجربة المشروع في منظومة باء: التهيئة والبناء والتشغيل والفحص
والاختبار والاعتماديات والحزم. لا يعيد تنفيذ المصرّف، ولا يطلب من الأدوات تحليل
رسائل باء البشرية حين تتوفر عقود JSON.

- **آخر تحديث:** 2026-08-01
- **الإصدار المنشور الحالي:** `0.1.0`
- **خط أساس مساحة Eco:** Baa `0.6.0`
- **الحالة:** أغلقت بوابة 0.4 الهندسية الأساسية على Windows وLinux؛ يبقى
  الإصدار المنشور `0.1.0` إلى أن تقطع حزمة إصدار مستقلة. تظل بنود 0.3 المؤجلة
  (globs والترحيل وتكامل Qalam) مراحل مستقلة ولا تدخل هذا الإغلاق.

## قرارات ثابتة

- المصدر مكتوب بلغة باء ليكون أداة dogfooding حقيقية.
- ملف المشروع وصفي وحتمي، وليس برنامج بناء قابلا للتنفيذ.
- العربية هي الصيغة القياسية؛ aliases الإنجليزية تخص CLI فقط.
- لا توجد أوامر lifecycle ضمنية عند تثبيت الحزم في الصيغة الأولى.
- `baalib` جزء من Baa SDK وليس حزمة عادية يحلها تكوين.
- يبدأ مدير الحزم باعتماديات المسار ثم Git المثبت ثم السجل العام لاحقا.
- يفصل تكوين بين ثلاثة نطاقات لا تكتب في بعضها: طقم Baa ثابت شبيه بتنظيم GCC،
  وأدوات مثبتة في بيئات مسماة، واعتماديات مشروع محكومة بالبيان والقفل.
- لا تنسخ حزمة طرف ثالث إلى طقم Baa، ولا يصبح مخزن الأدوات العام مصدرا ضمنيا
  لاعتماديات المشروع؛ كل مشروع يعيد إنتاج رسمه من `مشروع.تكوين` و`تكوين.قفل`.

## 0.1 — خط MVP الحالي

- [x] `تهيئة/init` وإنشاء مشروع بسيط.
- [x] parser صارم لصيغة `مفتاح: قيمة`.
- [x] `بناء/build` و`تشغيل/run` و`تنظيف/clean` على ويندوز.
- [x] مثبت Inno Setup وaliases عربية/إنجليزية.
- [x] إضافة `--إصدار/--version` وعقد تحقق آلي أساسي.
- [x] توحيد أكواد الخروج مع `compiler-cli-v1` من `0` إلى `5` وتمرير كود Baa كما هو.
- [x] إثبات check/build/run/clean عبر runner آلي على Baa 0.6.0.

## 0.2 — الأساس الحديث (مغلقة 2026-07-13)

- [x] تثبيت 0.6.0 كخط أساس 0.2 مثبت في smoke؛ انتقل تعريف مجالات الإصدارات
      العامة وفحصها إلى مسار SemVer في مدير الحزم.
- [x] استبدال تركيب `cmd /c` بواجهة عملية مهيكلة من Baa تستقبل argv وcwd
      دون إعادة تفسير shell.
- [x] إضافة smoke آلي لعقود parser وCLI وسلامة clean والبناء/التشغيل؛ تبقى
      اختبارات الوحدات بعد فصل النموذج عن globals.
- [x] دعم Windows وLinux من نموذج تنفيذ واحد؛ ثبت runner المشترك على
      `windows-latest` و`ubuntu-latest` في إيصال CI رقم 29251889635 بتاريخ 2026-07-13.
- [x] استهلاك `compiler-cli-v1` وأكواد الخروج عبر check/build/run/test دون تحليل النص.
- [x] تمرير وإتاحة `diagnostics-json-v1` عبر `فحص/check`.
- [x] استهلاك `--emit-build-manifest` وبيانات الاعتماديات والحالة incremental.
- [x] دعم اكتشاف الأهداف عبر `target-info-v1` بدلا من افتراض `.exe` دائما، مع تحقق
      قدرة الربط واستخدام لاحقة التنفيذ المعلنة من Baa.

**بوابة الخروج:** مشروع العينة نفسه يبنى ويعمل على Windows وLinux، ويخرج خطأ
المصدر بصيغة JSON مستقرة دون تحليل النص البشري.

**إيصال الإغلاق:** نجح Takween cross-platform smoke رقم
[29258386560](https://github.com/OmarAglan/Takween/actions/runs/29258386560)
على Windows وLinux، بما في ذلك تمرير أكواد `compiler-cli-v1` من `1` إلى `5`.
كما نجح Baa CI رقم
[29258365418](https://github.com/OmarAglan/Baa/actions/runs/29258365418)
ببوابات quick/full على النظامين، ونجح Qalam رقم
[29262024716](https://github.com/OmarAglan/Qalam-IDE/actions/runs/29262024716)
في استهلاك العقد وبناء/اختبار Windows وLinux دون تحليل الرسائل البشرية.

## 0.3 — `takween-manifest-v1`

- [x] parser بقيم typed: نص، صحيح، منطقي، ومصفوفة نصوص.
- [x] أقسام للمشروع وعدة أهداف والبناء والأنماط والاعتماديات ومساحة العمل،
      مع دلالات تنفيذية للأعضاء explicit وعقد فهرس مستقل.
- [x] أهداف `تنفيذي` و`مكتبة` و`اختبار` متعددة، مع اختيار build/check/run وتشغيل
      الاختبارات؛ يبقى إخراج المكتبة المستقلة للمرحلة التالية.
- [ ] توسعة globs بترتيب حتمي مع منع خروج المسار من جذر المشروع.
- [ ] ترحيل صريح من صيغة 0.1 دون تخمين غامض.
- [ ] Qalam يقرأ البيان عبر مخرجات تكوين المهيكلة، لا parser مستقل.

المواصفة: [`مستندات/MANIFEST_V1.md`](مستندات/MANIFEST_V1.md).

## 0.4 — DAG ومساحات العمل

- [x] جذور `.baa` و`.نظم` مختلطة، واختيار typed عربي
      `المجمع = "نظم"` يستهلك قدرة `nazm_source` ويستخدم الأمر العربي `نظم`
      من `PATH` بلا مسار أو علم لاتيني يكتبه المستخدم.
- [x] اعتماد نَظْم افتراضيا عند حذف `المجمع` مع تثبيت الاختيار في خطة البناء
      والقفل، وإبقاء `المجمع = "غاز"` رجوعا صريحا بلا fallback تلقائي.
- [x] خطة بناء مملوكة وحتمية مستقلة عن التنفيذ الجانبي، مع عقد
      `takween-build-plan-v1` وأمر `خطة --جسون` لا يشغّل المصرّف.
- [x] DAG صريح للأهداف عبر `يعتمد_على` مع ترتيب حتمي dependency-first، دمج
      مصادر المكتبات في الخطة، وكشف دورة يطبع المسار الكامل.
- [x] أنماط `تطوير` و`إصدار` typed قابلة للتوسعة بأسماء عربية مخصصة، واختيار
      `--نمط` من CLI وإيصال profile مملوك داخل خطة البناء.
- [x] cache حتمي بوحدات Baa مع إيصال `takween-content-cache-v1` معنون
      بـSHA-256 من إصدار المصرّف والهدف والأعلام وترتيب المدخلات وتجزئات
      المصادر والرؤوس والقفل وبصمة `nazm-api-v1`؛ الربط يبقى صريحا، ووحدات
      باء و`.نظم` تعاد فقط مع تطابق إصدار نظم وهضم قدراته.
- [x] مساحات عمل متعددة الحزم بعقد `takween-workspace-v1`، وأعضاء explicit
      آمنين وفريدين، وبناء/اختبار انتقائي أو `--كل_الحزم` بترتيب التصريح،
      وتمرير النمط/القفل وكود الخروج عبر عملية مهيكلة لكل عضو.
- [x] عقد `اختبار/test` وخرج `takween-targets-v1` ثابت لتكامل Qalam وCI.

**إيصال cache:** نفذ commit
[`2b9c032`](https://github.com/OmarAglan/Takween/commit/2b9c032107dad0ce35d35b6c011398d1279925eb)
اشتقاق المفتاح والإيصالات واختبارات invalidation/hit. نجح
[Takween cross-platform smoke رقم 29643531440](https://github.com/OmarAglan/Takween/actions/runs/29643531440)
على `windows-latest` و`ubuntu-latest`.

**إيصال مساحة العمل وإغلاق 0.4:** نفذ commit
[`d2d1f7a`](https://github.com/OmarAglan/Takween/commit/d2d1f7aacf4a53e1758d1a3b1318b007772c4cc3)
الفهرس والاختيار والعزل وتمرير الأكواد واختبارات الضغط. نجح
[Takween cross-platform smoke رقم 29644762434](https://github.com/OmarAglan/Takween/actions/runs/29644762434)
على `windows-latest` و`ubuntu-latest`.

**إيصال قبول نَظْم الافتراضي:** نفذ commit
[`4fe634f`](https://github.com/OmarAglan/Takween/commit/4fe634f92abf9658a21b6ce5bf80b91e08958877)
اختيار نَظْم الافتراضي والرجوع الصريح إلى غاز وتثبيت مراجعات Baa/Nazm.
أعاد commit
[`da8378e`](https://github.com/OmarAglan/Takween/commit/da8378e097ef0f98acd19bfc76da7acf445547af)
تثبيت smoke على مرشح باء الدقيق `661edd9...` ونَظْم `7236491...`. نجح
[smoke رقم 29689709002](https://github.com/OmarAglan/Takween/actions/runs/29689709002)
على Windows وLinux بعد نجاح
[قبول Baa/Nazm رقم 29687846586](https://github.com/OmarAglan/Baa/actions/runs/29687846586).

**امتداد بصمة نظم والكاش:** يثبت workflow الحالي Baa
`4e250dad97d9d64de7f69ca96c8be48a34dc9ff5` ونظم
`f7fcf8f6d2bf629daf708b3b6028e22c74683ce6`. يختبر أول miss ثم hit لوحدة باء
مولدة ووحدة `.نظم` مباشرة، ويثبت أن البصمة الكاملة جزء من هوية المحتوى. نفذ
commit [`5cf82c3`](https://github.com/OmarAglan/Takween/commit/5cf82c3062f20d1dfa5584960cdf93120fe6ba96)
العقد، ونجح [smoke رقم 29920746642](https://github.com/OmarAglan/Takween/actions/runs/29920746642)
نهائيا على Windows وLinux.

## 0.5 — اعتماديات محلية وملف قفل

- [x] اعتماديات `مسار` محلية ومتعدية مع إضافة المصادر والتضمينات وكشف الدورات.
- [x] اعتماديات Git مع commit مثبت، checkout بلا shell، وcache محلي يعاد استخدامه offline.
- [x] محلل Semantic Versioning حتمي، مع اختيار أعلى إصدار مطابق ورسائل تعارض
      صريحة وقيد كامل `major.minor.patch` ومقارنات exact/`^`/`~`/`<`/`<=`/`>`/`>=`.
- [x] أساس `takween-lock-v1` مولد وحتمي للمشروع والهدف وعقد path/Git المتعدية
      ومصادرها وcommit الدقيق.
- [x] `takween-archive-v1` immutable مع SHA-256 لكل أرشيف وملف، واستخراج محدود
      يرفض absolute/`..`/الروابط والتكرار وتصادم ملف/مجلد والفرز أو الحدود الخاطئة.
- [x] cache archive معنون بالتجزئة وإيصال استخراج يعاد فحصه، مع
      `تحقق --بدون_شبكة` صارم و`توريد` حتمي إلى `مورد/<اسم>/<إصدار>/<sha256>`.
- [x] تطبيق قيد إصدار Baa والهدف وقدرة `تجميع` أثناء الحل، وقدرات
      `كائن/ربط/مكتبة_باء` قبل build، وتسجيل القيود في `takween-lock-v1`.
- [x] حفظ اختيار SemVer النصي الدقيق وSHA-256 ومسار الفهرس والأرشيف في القفل،
      مع فهرس محلي من أربعة حقول لا يثق في `content_path` خارجي.
- [x] أسماء مشروع/حزمة/alias عربية فقط: حروف عربية وأرقام عربية وشرطة سفلية،
      مع رفض اللاتينية والخلط والحركات والتطويل وbidi controls.
- [ ] تطبيع NFC صريح ومهاجرة هوية إذا احتاج السجل المستقبلي مقارنة canonical؛
      لا يغير v1 البايتات بصمت.

## 0.6 — أطقم Baa وبيئات الأدوات

**القرار المعماري:** يستعير توزيع المصرّف من GCC فكرة طقم ثابت قابل للاكتشاف
بجذر نظام وملفات هدف ومكتبات تشغيل، بينما يملك تكوين التثبيت والمعاملات والبيئات.
يبقى حل مكتبات المشروع محليا ومحكوما بالقفل كما في 0.5.

- [ ] تعريف تخطيط طقم Baa ثابت ومؤرشف بالإصدار: المصرّف، ونَظْم، و`baalib`،
      وترويسات SDK، ومكتبات التشغيل، وملفات كل هدف، مع أمر مهيكل لاكتشاف المسارات.
- [ ] اختيار إصدار Baa والهدف من البيان والقفل، وتثبيت بصمات المصرّف ونَظْم
      وملفات الهدف دون طلب معرفة المستخدم بالمسارات الفيزيائية.
- [ ] دعم عدة إصدارات من طقم Baa جنبا إلى جنب دون استبدال متبادل أو كتابة
      اعتماديات المشروع داخل أي طقم.
- [ ] إضافة مخزن أدوات معنون بالمحتوى وبيئات مسماة تعرض الأدوات عبر روابط/مشغلات
      مولدة حتميا على Windows وLinux.
- [ ] إضافة قاعدة بيانات تثبيت معاملية تسجل الحزمة والإصدار والتجزئة والبيئة
      وكل ملف مملوك، وترفض تعارض الملفات قبل التعديل وتسترجع الحالة عند الفشل.
- [ ] إضافة الأوامر العربية `بيئة` و`ثبت` و`أزل` و`قائمة` و`مالك` للأدوات، مع
      أحداث JSON مهيكلة يستهلكها Qalam وCI ودون lifecycle scripts ضمنية.
- [ ] إثبات أن حذف أداة أو بيئة لا يحذف ملفا مشتركا ما زال مملوكا، وأن مشروعين
      بقفلين مختلفين يبنيان بصورة مستقلة عن البيئة العامة النشطة.

**بوابة الخروج:** يثبت تكوين إصدارين من طقم Baa جنبا إلى جنب، وينشئ بيئة أدوات
ويثبت/يزيل منها أداة بمعاملة قابلة للاسترجاع، ثم يعيد بناء مشروع مقفل على
Windows وLinux دون تغيير طقم SDK أو الاعتماد على حالة عامة غير مقفلة.

## 0.7+ — عميل السجل والنشر

- [ ] غلاف نشر وتوقيع metadata فوق `takween-archive-v1` المحلي المنفذ.
- [ ] namespaces وملكية الاسم وmetadata الترخيص والهدف وإصدار Baa.
- [ ] فهارس بعيدة موقعة بلقطات غير قابلة للتبديل، مع تدوير المفاتيح وإلغاء
      الثقة وسياسة سحب لا تعيد استخدام اسم/إصدار منشور لمحتوى مختلف.
- [ ] غلاف نقل مضغوط جديد يحتفظ بدلالات قائمة الملفات والتجزئات والاستخراج
      المحدود في `takween-archive-v1` دون حمولة hex مضاعفة الحجم.
- [ ] `ابحث` و`أضف` و`احذف` و`حدّث` لاعتماديات المشروع؛ تبقى `ثبت` للأطقم
      والأدوات حتى لا تختلط حالة المشروع بالحالة العامة.
- [ ] `انشر/publish` مع checksum وإثبات المصدر وسياسة سحب آمنة.
- [ ] سجل عام فقط بعد استقرار الصيغة والقفل وعقود Baa SDK/conformance.

معمارية الحزم: [`مستندات/PACKAGES.md`](مستندات/PACKAGES.md).

## تكامل Qalam

- [x] اكتشاف `مشروع.تكوين` من Qalam.
- [x] build/run/clean/test عبر تكوين للمشاريع.
- [x] direct Baa check سريع للتشخيصات والرموز داخل المحرر.
- [x] منتج `takween-build-events-v1` كـJSONL في ملف صريح لأحداث العملية
      والمراحل والأهداف والحزم والآثار، دون كسر `stdout/stderr` العربية.
- [x] يستهلك Qalam `takween-targets-v1` و`takween-build-events-v1` لعرض الهدف
      والتقدم ودورة الحياة والإلغاء دون تحليل النص.

## PyramidOS

لا يدخل تكامل النواة في تنفيذ هذه الخارطة قبل عبور PyramidOS بوابات v0.9.
بعدها يبدأ profile وصفي يبني bootloader والكائنات وlinker script والصورة وQEMU،
ولا يفرض افتراضات ويندوز أو CRT على المشروع.
